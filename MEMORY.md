Project notes and decisions for anuraggurumurthy (GitHub profile repo), not otherwise captured in [README.md](README.md) or [CLAUDE.md](CLAUDE.md).

## 2026-07-13

Initial CLAUDE.md/MEMORY.md documentation pass — this repo is just a GitHub profile README (badges, tech stack, socials, resume link) plus a release-tagging GitHub Action and a same-named Windows service (`scripts/StartService.bat`/`StopService.bat`) that, based on the git history of "Auto commit" messages updating `release notes/Latest Release.txt` with a build timestamp, appears to auto-commit that file on some local schedule — the mechanism behind those commits isn't otherwise documented in the repo.
